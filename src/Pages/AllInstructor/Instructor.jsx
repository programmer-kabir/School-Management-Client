import React from 'react';

const Instructor = () => {
    const {
        isLoading,
        classData,
        review,
      } = useSelector((state) => state.class);
    return (
        <div>
            
        </div>
    );
};

export default Instructor;